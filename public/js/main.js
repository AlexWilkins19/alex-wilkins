var button = document.getElementById('about');

button.onclick= function() {

    // this adds the 'active' class to the classes that the element already has
    var maptab = document.getElementById('about');

    maptab.className = maptab.className + 'active';
};

var button = document.getElementById('music');

button.onclick= function() {

    // this adds the 'active' class to the classes that the element already has
    var maptab = document.getElementById('music');

    maptab.className = maptab.className + 'active';
};

var button = document.getElementById('portfolio');

button.onclick= function() {

    // this adds the 'active' class to the classes that the element already has
    var maptab = document.getElementById('portfolio');

    maptab.className = maptab.className + 'active';
};

var button = document.getElementById('blog');

button.onclick= function() {

    // this adds the 'active' class to the classes that the element already has
    var maptab = document.getElementById('blog');

    maptab.className = maptab.className + 'active';
};
